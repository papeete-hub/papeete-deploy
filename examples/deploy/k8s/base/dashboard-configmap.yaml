apiVersion: v1
kind: ConfigMap
metadata:
  name: dashboard
  labels:
    grafana_dashboard: "1"
data:
  pd-table-service.json: |
    {
      "title": "pd-table-service — customer/waiter drill-down",
      "templating": {
        "list": [
          {
            "name": "actor",
            "type": "custom",
            "options": [
              {"text": "customer", "value": "customer"},
              {"text": "waiter", "value": "waiter"}
            ]
          }
        ]
      },
      "panels": []
    }
